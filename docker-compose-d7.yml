drupal:
  image: drupal-tester
  volumes:
    - ./test_results:/var/www/html/test_results
  environment:
    - DRUPAL_VERSION=7.54
    - MODULES_DOWNLOAD=module-version
    - MODULES_ENABLE=module
    - SIMPLETEST_GROUPS=module test group
